# Zynotch / Fintap — 1BILL handover and compliance status

Prepared: 8 September 2026  
Service: 1BILL invoice-based aggregator, prefix 105172, settlement T+1 via UBL

## Information to send to the 1LINK representative

| Requested item | Value / delivery instruction |
|---|---|
| Public VPN/host IP | `178.238.236.126` |
| Private/protected application IP | `172.29.250.10/32` (dedicated VPN traffic selector). The existing Contabo address `10.0.0.1/22` remains unchanged. |
| Application port | TCP `443` over HTTPS through the IPsec tunnel |
| REST URLs | `https://app.fintap.pk/api/1.0/Payments/BillInquiry` and `https://app.fintap.pk/api/1.0/Payments/BillPayment` |
| Username | Share the configured `ONELINK_USERNAME` through the agreed channel. Current known label is `1bill-user`; confirm before sending. |
| Password | Send the configured `ONELINK_PASSWORD` through a separate secure channel. Do not place it in this document or ordinary email. |
| SSL certificate | Send `app.fintap.pk-leaf.crt`; include `app.fintap.pk-fullchain.crt` if they request the chain. Never send `privkey.pem`. |
| Prefix | `105172` |
| Our peer configuration | 1LINK public peer `103.248.140.4`; IKEv2, AES-256, SHA-256, DH group 19, PFS enabled. PSK is already stored and must not be disclosed. |
| 1LINK protected hosts | `10.95.8.92/32` and `10.95.8.94/32`; HTTPS TCP 443 allowed at network and application layers. |
| UAT consumers | Provide the reviewed CSV generated by `server/scripts/provision_1bill_uat.js`: 14 unpaid, 2 paid, 1 after-due, 2 blocked, and 1 exactly 24 digits. Confirm official slab boundaries first because the supplied ranges overlap and list only six unambiguous ranges for seven required slabs. |

## Message to send now

> We have configured our IKEv2/IPsec endpoint at public IP 178.238.236.126 with protected application IP 172.29.250.10/32. Our REST service is available over TCP 443 at the two invoice URLs listed above. Our gateway is transmitting IKE_SA_INIT to your peer 103.248.140.4, but we have not received a response. Please confirm that your VPN configuration is active, that 178.238.236.126 is whitelisted, and that your traffic selectors are 172.29.250.10/32 to 10.95.8.92/32 and 10.95.8.94/32. Please also confirm the UAT and production sources are separate, the exact UAT bank mnemonic/UCID, the approved cipher string, whether FetchBundle is excluded from our invoice-only scope, and whether the 24-digit test consumer is supported despite the specification note that 1LINK normally accepts a maximum of 20 digits.

Send the username, password and certificate as separate protected attachments/messages according to 1LINK security instructions.

## Application contract compliance matrix

| Requirement from local 1LINK REST v1.5 document | Implementation status | Evidence / note |
|---|---|---|
| BillInquiry POST URL and JSON fields | Implemented | Exact route, consumer/bank/reserved validation and response envelope exist. |
| BillPayment POST URL and JSON fields | Implemented | Numeric six-digit auth ID, 12-digit minor-unit amount, dates/times, mnemonic and reserved validation. |
| Header username/password | Implemented | Constant-time comparison; secrets loaded from environment only. |
| Source network restriction | Implemented locally | In production only configured 1LINK IPs pass. Final proof requires traffic through the established tunnel. |
| Consumer detail A30 | Implemented | Truncated to 30 and right-padded. |
| Due date yyyyMMdd | Implemented | UTC-normalized formatting. |
| Inquiry amount AN14 | Implemented | Sign plus 13 minor-unit digits. |
| Payment amount AN12 | Implemented | 12 numeric minor-unit digits and exact-payable comparison. |
| Billing month yyMM | Implemented | Four numeric digits. |
| Paid fields | Implemented | Date, amount and six-character transaction reference returned for paid consumers. |
| Blocked consumer | Implemented | Response code 02 with bill status B. |
| Duplicate payment | Implemented | Unique key is consumer + auth ID + date + time; duplicate code 03. |
| Already-paid payment | Implemented | Code 06 is returned by the extension documented in the response summary. |
| Atomic evidence | Implemented | Payment, transaction, allocation, invoice/status, ledger, audit and outbox commit together. |
| TLS 1.2 / port 443 / CA certificate | Verified previously on VM | Nginx serves the CA-authorized certificate and TLS 1.2/1.3. Before submission, remove any active TLS 1.0/1.1 setting and verify the exact highlighted cipher from 1LINK. |
| IPsec AES-256/SHA-256/DH19/PFS | Configured locally | StrongSwan sent IKE_SA_INIT; peer did not reply. Not established/certified yet. |
| FetchBundle | Intentionally retired | The project kickoff says invoice services and the product owner requested removal, but REST v1.5 lists FetchBundle. Obtain written 1LINK confirmation before production cutover. |
| Consumer length | Supports 14, grandfathered 20, and 24 | REST document says field max 24 but separately notes 1LINK max 20. The requested 24-digit UAT case requires written acceptance. |

## Remaining 1LINK-side steps

1. Activate/configure peer `103.248.140.4` and whitelist `178.238.236.126`.
2. Confirm Phase 1/2 proposals, traffic selectors, NAT-T behavior and approved cipher/certificate expectations.
3. Provide separate UAT and production gateway details: peer/protected IPs if different, mnemonic/UCID, credentials and test window.
4. Confirm invoice-only scope excludes FetchBundle and clarify 24-digit handling.
5. Confirm the official seven non-overlapping amount slabs and accept the 20-consumer sheet.
6. Run connectivity checks, REST contract UAT, duplicate/paid/blocked/after-due cases, sign-off, production cutover and settlement/SFTP verification.

## Remaining Zynotch-side steps

1. Send public/protected IPs, port, URLs, certificate and credentials as described above.
2. Verify Nginx has no effective TLS 1.0/1.1 configuration and that the exact cipher required in the attached image is enabled.
3. After 1LINK confirms activation, capture `swanctl --list-sas`, StrongSwan logs and packet evidence showing inbound negotiation and established CHILD SAs.
4. Rehearse the application migration on a production copy; deploy API plus outbox worker using the runbook.
5. Provision the physically separate sandbox and complete load, restore and reconciliation drills.
6. Complete joint UAT and retain written 1LINK and business approvals before accepting real payments.

## Safe VPN retest after 1LINK confirmation

```bash
swanctl --initiate --child onebill-92
swanctl --list-sas
journalctl -u strongswan --since '-10 minutes' --no-pager
tcpdump -ni eth0 'host 103.248.140.4 and (udp port 500 or udp port 4500 or ip proto 50)'
```

Do not change the working loopback address, production routes, Nginx listener, PSK or CHILD selectors merely because the peer is silent at IKE_SA_INIT.
