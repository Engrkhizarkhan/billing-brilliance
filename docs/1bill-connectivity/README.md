# 1BILL connectivity package

Send these three files to the 1LINK project team:

1. `1BILL_CONNECTIVITY_SUBMISSION_2026-09-05.md`
2. `app.fintap.pk-leaf.crt`
3. `app.fintap.pk-fullchain.crt`

Share the web-service password separately. Do not paste it into the email or the Markdown submission.

The IPsec tunnel is not yet complete because the VM has no private IP and the supplied template leaves the customer protected address and pre-shared key blank. The submission asks 1LINK to confirm those required values.
